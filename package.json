{
  "name": "segim",
  "version": "1.0.0",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/TylerWasniowski/segim.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "concurrently \"cd backend && node server.js\" \"cd client && yarn build && yarn global add serve && serve -s -l 443 build\"",
    "install": "cd backend && yarn install && cd .. && cd client && yarn install",
    "start": "concurrently \"cd backend && node server.js\" \"cd client && yarn start\""
  },
  "dependencies": {
    "concurrently": "^4.1.2"
  }
}
